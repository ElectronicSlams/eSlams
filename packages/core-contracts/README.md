# @eslams/core-contracts

In-tree contracts package. `package.json` is version `0.6.1`. `"private": true`
blocks an accidental `npm publish`.

Do not `npm publish` `@eslams/core-contracts` until a founder gate. This package
is not published to npm. Consumers of published Python `eslams-core==0.6.1` are
unaffected.
